# MiniMap (full-track overview strip) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Revised after plan-review.** The first draft cached a rendered *offscreen canvas* and counted `e.touches` in the gesture handler. Both were wrong: the expensive part is walking the peaks, not stroking ~390 segments (`WaveformCanvas` already strokes that many per frame at 50vh), and a global touch count dead-locks the strip when a second finger lands on a sibling canvas. This version caches the **downsampled array** and tracks the **touch identifier**.

**Goal:** Тонкий обзорный стрип под основной волной: вся дорожка целиком, позиция playhead, A-B луп, маркеры; тап/драг = seek.

**Architecture:** Две новые чистые функции (`downsamplePeaks` в `computePeaks.ts`, `overviewTimeToX`/`overviewXToTime` в `viewport.ts`) — TDD. Компонент `MiniMap.tsx` — императивный canvas по паттерну `WaveformCanvas`: rAF-цикл читает стор через `store.getState()`. Full-track пики (200 бакетов/с — десятки тысяч на минуты) **не обходятся каждый кадр**: `downsamplePeaks` считается один раз на `(peaks, columns)` и кешируется как `Float32Array`; отрисовка ~`cssW` сегментов из кеша дешева. Жесты — по `Touch.identifier` (не по счётчику), пауза откладывается до первого движения, чтобы голый тап не рестартовал source дважды.

**Tech Stack:** React 19, TS 7 (tsgo), zustand 5, Canvas 2D, Vitest 4. Без новых зависимостей.

## Global Constraints

- Только мобильные (portrait). Стрип 48px, тач-зона по всей ширине.
- Пур-логика — тест-первый. Canvas + жесты — сборка + ручная проверка (политика проекта, jsdom без canvas).
- MiniMap **не** зовёт `store.tick()` — тик принадлежит `WaveformCanvas` (единственный владелец; два тикера дважды опрашивали бы часы движка). MiniMap читает `position` из стора и потому максимум на кадр «отстаёт» — незаметно.
- Стор читаем императивно (`store.getState()`), НЕ через React-селектор: `position` меняется 60/с, подписка ре-рендерила бы стрип каждый кадр.
- Пики: интерливед `[min0,max0,min1,max1,…]`, длина = `2 × buckets`.
- Размер бэкинг-стора canvas сравнивать через `Math.round(cssW * dpr)`: `canvas.width` — `unsigned long`, при дробном dpr (Android 2.625/2.75) прямое сравнение с `cssW*dpr` всегда истинно → реаллокация каждый кадр.

---

## Task 1: `downsamplePeaks` (pure)

**Files:**
- Modify: `src/waveform/computePeaks.ts`
- Test: `src/waveform/computePeaks.test.ts`

**Interfaces:**
- Produces:
  ```ts
  // Reduce an interleaved [min,max] peaks array down to exactly `columns`
  // interleaved [min,max] pairs (one per pixel column of the minimap).
  function downsamplePeaks(peaks: Float32Array, columns: number): Float32Array;
  ```
- Consumed by `MiniMap` (Task 3).

- [ ] **Step 1: Write the failing tests — append to `src/waveform/computePeaks.test.ts`**

Extend the existing import line:

```ts
import { computePeaks, downsamplePeaks } from './computePeaks';
```

Append a new `describe` block (every assertion pins exact values — an all-zeros
array must NOT pass):

```ts
describe('downsamplePeaks', () => {
  it('reduces to exactly the requested number of columns', () => {
    const peaks = new Float32Array(200 * 2);
    expect(downsamplePeaks(peaks, 50).length).toBe(50 * 2);
  });

  it('keeps the min and max across each column span', () => {
    // 4 buckets -> 2 columns, each column spans 2 buckets.
    const peaks = new Float32Array([-0.1, 0.2, -0.9, 0.4, -0.2, 0.8, -0.3, 0.1]);
    const out = downsamplePeaks(peaks, 2);
    expect(Array.from(out)).toEqual([-0.9, 0.4, -0.3, 0.8].map((v) => Math.fround(v)));
  });

  it('handles a span that does not divide evenly (5 buckets -> 2 columns)', () => {
    // spans: col0 = buckets [0,2), col1 = buckets [2,5)
    const peaks = new Float32Array([
      -0.1, 0.1, // b0
      -0.7, 0.2, // b1
      -0.2, 0.9, // b2
      -0.3, 0.4, // b3
      -0.5, 0.6, // b4
    ]);
    const out = downsamplePeaks(peaks, 2);
    expect(Array.from(out)).toEqual([-0.7, 0.2, -0.5, 0.9].map((v) => Math.fround(v)));
  });

  it('duplicates buckets when there are fewer than columns (no empty columns)', () => {
    // 2 buckets -> 4 columns: cols 0,1 take bucket 0; cols 2,3 take bucket 1.
    const peaks = new Float32Array([-0.5, 0.5, -0.25, 0.25]);
    const out = downsamplePeaks(peaks, 4);
    expect(Array.from(out)).toEqual(
      [-0.5, 0.5, -0.5, 0.5, -0.25, 0.25, -0.25, 0.25].map((v) => Math.fround(v)),
    );
  });

  it('returns empty for empty peaks or non-positive columns', () => {
    expect(downsamplePeaks(new Float32Array(0), 10).length).toBe(0);
    expect(downsamplePeaks(new Float32Array([0, 1]), 0).length).toBe(0);
  });
});
```

> `Math.fround` is used because `Float32Array` stores single-precision values —
> `-0.9` round-trips to `-0.8999999761581421`, so a raw `toEqual([-0.9, …])` would fail.

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run src/waveform/computePeaks.test.ts`
Expected: FAIL (`downsamplePeaks` is not exported).

- [ ] **Step 3: Implement — append to `src/waveform/computePeaks.ts`**

```ts
// Reduce the full-track peaks to one [min,max] pair per pixel column of the
// minimap. A full track holds PEAKS_RESOLUTION buckets/sec (tens of thousands
// for a few minutes), so the minimap must not walk them every frame — it
// downsamples once per (peaks, columns) and caches the result (see MiniMap.tsx).
export function downsamplePeaks(peaks: Float32Array, columns: number): Float32Array {
  const buckets = Math.floor(peaks.length / 2);
  if (buckets === 0 || columns <= 0) return new Float32Array(0);
  const out = new Float32Array(columns * 2);
  for (let c = 0; c < columns; c++) {
    // Column -> bucket span. `max(start + 1, …)` guarantees at least one bucket
    // per column, so a track with fewer buckets than columns fills every column
    // (buckets get duplicated) instead of leaving zeroed gaps.
    const start = Math.floor((c * buckets) / columns);
    const end = Math.max(start + 1, Math.floor(((c + 1) * buckets) / columns));
    let min = peaks[start * 2];
    let max = peaks[start * 2 + 1];
    for (let b = start + 1; b < end && b < buckets; b++) {
      const lo = peaks[b * 2];
      const hi = peaks[b * 2 + 1];
      if (lo < min) min = lo;
      if (hi > max) max = hi;
    }
    out[c * 2] = min;
    out[c * 2 + 1] = max;
  }
  return out;
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npx vitest run src/waveform/computePeaks.test.ts`
Expected: PASS (existing 3 + 5 new).

- [ ] **Step 5: Commit**

```bash
git add src/waveform/computePeaks.ts src/waveform/computePeaks.test.ts
git commit -m "feat: downsamplePeaks for minimap columns"
```

---

## Task 2: Overview viewport math (pure)

**Files:**
- Modify: `src/waveform/viewport.ts`
- Test: `src/waveform/viewport.test.ts`

**Interfaces:**
- Produces:
  ```ts
  function overviewTimeToX(t: number, duration: number, width: number): number;
  function overviewXToTime(x: number, duration: number, width: number): number; // clamped 0..duration
  ```
- Consumed by `MiniMap` (Task 3).

- [ ] **Step 1: Write the failing tests — append to `src/waveform/viewport.test.ts`**

Extend the existing import:

```ts
import {
  timeToX,
  xToTime,
  clampPxPerSec,
  panDeltaToTime,
  overviewTimeToX,
  overviewXToTime,
} from './viewport';
```

Append:

```ts
describe('overview viewport (minimap)', () => {
  it('maps time linearly across the full width', () => {
    expect(overviewTimeToX(0, 100, 400)).toBeCloseTo(0);
    expect(overviewTimeToX(50, 100, 400)).toBeCloseTo(200);
    expect(overviewTimeToX(100, 100, 400)).toBeCloseTo(400);
  });

  it('overviewXToTime is the inverse of overviewTimeToX', () => {
    const x = overviewTimeToX(37, 100, 400);
    expect(overviewXToTime(x, 100, 400)).toBeCloseTo(37);
  });

  it('overviewXToTime clamps to 0..duration', () => {
    expect(overviewXToTime(-50, 100, 400)).toBe(0);
    expect(overviewXToTime(9999, 100, 400)).toBe(100);
  });

  it('is safe when duration or width is zero', () => {
    expect(overviewTimeToX(10, 0, 400)).toBe(0);
    expect(overviewXToTime(200, 100, 0)).toBe(0);
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run src/waveform/viewport.test.ts`
Expected: FAIL (not exported).

- [ ] **Step 3: Implement — append to `src/waveform/viewport.ts`**

```ts
// Minimap mapping: the WHOLE track spans the strip width (as opposed to
// timeToX/xToTime above, which centre a zoomed window on the playhead).
export const overviewTimeToX = (t: number, duration: number, width: number): number =>
  duration > 0 ? (t / duration) * width : 0;

export const overviewXToTime = (x: number, duration: number, width: number): number => {
  if (width <= 0 || duration <= 0) return 0;
  return Math.min(duration, Math.max(0, (x / width) * duration));
};
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npx vitest run src/waveform/viewport.test.ts`
Expected: PASS (existing 5 + 4 new).

- [ ] **Step 5: Commit**

```bash
git add src/waveform/viewport.ts src/waveform/viewport.test.ts
git commit -m "feat: overview (minimap) time<->x math"
```

---

## Task 3: MiniMap component

**Files:**
- Create: `src/waveform/MiniMap.tsx`

**Interfaces:**
- Consumes: `downsamplePeaks` (Task 1), `overviewTimeToX`/`overviewXToTime` (Task 2), store (`peaks`, `duration`, `position`, `loopStart`, `loopEnd`, `markers`, `playing`, `seek`, `togglePlay`).
- Produces: `<MiniMap />` — mounted by Task 4.

> Canvas + touch has no unit test (jsdom has no real canvas — project policy). Verified by build + manual device check in Task 4.

**Three review-mandated properties, do not "simplify" them away:**
1. **Cache the downsampled array, not a rendered canvas.** Walking ~48k buckets per frame is the cost; stroking ~390 segments is not (`WaveformCanvas` already does that every frame at 50vh).
2. **Gestures key on `Touch.identifier`, never on `e.touches.length`.** MiniMap only receives events for touches that *started on the strip*. A second finger landing on the sibling `WaveformCanvas` would make a length-based `onEnd` bail forever, leaving `dragging`/`wasPlaying` stuck true — playback paused with the strip permanently dead.
3. **Pause is deferred to the first `touchmove`.** Pausing on `touchstart` would make a bare tap do `pause()` → `seek()` → `play()`, restarting the audio source twice.

- [ ] **Step 1: Write `src/waveform/MiniMap.tsx`**

```tsx
import { useEffect, useRef } from 'react';
import { usePlayerStore } from '../store/usePlayerStore';
import { overviewTimeToX, overviewXToTime } from './viewport';
import { downsamplePeaks } from './computePeaks';
import type { Marker } from '../types';

export function MiniMap() {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const store = usePlayerStore;

  // rAF render loop. Reads the store imperatively (never via a React selector):
  // `position` changes ~60/s, so a subscription would re-render every frame.
  // NOTE: this loop must NOT call store.tick() — WaveformCanvas owns the tick.
  // (If WaveformCanvas is ever unmounted while MiniMap stays mounted, this
  // playhead would freeze — they are mounted together in Player.)
  useEffect(() => {
    const canvas = canvasRef.current!;
    const g = canvas.getContext('2d')!;
    let raf = 0;

    // Cache of the DOWNSAMPLED peaks (one [min,max] per pixel column). Rebuilt
    // only when the track (peaks ref) or the column count changes.
    let cols: Float32Array | null = null;
    let colsForPeaks: Float32Array | null = null;
    let colsCount = 0;

    // Dirty check: both this and WaveformCanvas run their own rAF loop, so skip
    // the draw body entirely when nothing visible changed (idle/paused battery).
    let lastPos = NaN;
    let lastA: number | null = null;
    let lastB: number | null = null;
    let lastMarkers: Marker[] | null = null;
    let lastPeaks: Float32Array | null = null;
    let lastW = -1;
    let lastH = -1;

    const draw = () => {
      const dpr = window.devicePixelRatio || 1;
      const cssW = canvas.clientWidth;
      const cssH = canvas.clientHeight;
      const { peaks, duration, position, loopStart, loopEnd, markers } = store.getState();

      const clean =
        position === lastPos &&
        loopStart === lastA &&
        loopEnd === lastB &&
        markers === lastMarkers &&
        peaks === lastPeaks &&
        cssW === lastW &&
        cssH === lastH;
      if (clean) {
        raf = requestAnimationFrame(draw);
        return;
      }
      lastPos = position;
      lastA = loopStart;
      lastB = loopEnd;
      lastMarkers = markers;
      lastPeaks = peaks;
      lastW = cssW;
      lastH = cssH;

      // Round: canvas.width is an unsigned long, so comparing it against a
      // fractional cssW*dpr (Android dpr 2.75) is always true -> the backing
      // store would be reallocated every single frame.
      const bw = Math.round(cssW * dpr);
      const bh = Math.round(cssH * dpr);
      if (canvas.width !== bw || canvas.height !== bh) {
        canvas.width = bw;
        canvas.height = bh;
      }
      g.setTransform(dpr, 0, 0, dpr, 0, 0);
      g.clearRect(0, 0, cssW, cssH);

      if (cssW <= 0 || cssH <= 0) {
        raf = requestAnimationFrame(draw);
        return;
      }

      // waveform (whole track, from the column cache)
      if (peaks && peaks.length > 0) {
        const n = Math.max(1, Math.floor(cssW));
        if (cols === null || colsForPeaks !== peaks || colsCount !== n) {
          cols = downsamplePeaks(peaks, n);
          colsForPeaks = peaks;
          colsCount = n;
        }
        const mid = cssH / 2;
        g.strokeStyle = '#3f6ea8';
        g.lineWidth = 1;
        g.beginPath();
        for (let c = 0; c < n; c++) {
          const min = cols[c * 2];
          const max = cols[c * 2 + 1];
          const x = c + 0.5;
          g.moveTo(x, mid - max * mid);
          g.lineTo(x, mid - min * mid);
        }
        g.stroke();
      }

      // loop region
      if (loopStart !== null && loopEnd !== null && loopEnd > loopStart) {
        const xa = overviewTimeToX(loopStart, duration, cssW);
        const xb = overviewTimeToX(loopEnd, duration, cssW);
        g.fillStyle = 'rgba(90,160,255,0.22)';
        g.fillRect(xa, 0, xb - xa, cssH);
      }

      // playhead
      const px = overviewTimeToX(position, duration, cssW);
      g.strokeStyle = '#ff5a5a';
      g.lineWidth = 2;
      g.beginPath();
      g.moveTo(px, 0);
      g.lineTo(px, cssH);
      g.stroke();

      // markers LAST — same convention as WaveformCanvas: at minimap scale the
      // whole track is ~390px wide, so a marker near the playhead would be
      // swallowed by the red line if drawn under it.
      g.strokeStyle = '#ffcf5a';
      g.lineWidth = 1;
      for (const m of markers) {
        const x = overviewTimeToX(m.time, duration, cssW);
        g.beginPath();
        g.moveTo(x, 0);
        g.lineTo(x, cssH);
        g.stroke();
      }

      raf = requestAnimationFrame(draw);
    };
    raf = requestAnimationFrame(draw);
    return () => cancelAnimationFrame(raf);
  }, [store]);

  // Gestures: tap or drag anywhere on the strip seeks to that point in the track.
  //
  // Tracked by Touch.identifier, NOT by e.touches.length: MiniMap only gets
  // events for touches that STARTED on it, so a second finger landing on the
  // sibling WaveformCanvas would never show up in a length check — onEnd would
  // bail forever and leave the strip dead with playback stuck paused.
  //
  // Pause is deferred to the first touchmove so a bare tap is a single seek
  // (pausing on touchstart would restart the audio source twice: pause+play).
  useEffect(() => {
    const canvas = canvasRef.current!;
    let activeId: number | null = null;
    let pausedByDrag = false;
    let wasPlaying = false;

    const seekToClientX = (clientX: number) => {
      const s = store.getState();
      const rect = canvas.getBoundingClientRect();
      s.seek(overviewXToTime(clientX - rect.left, s.duration, rect.width));
    };

    const findActive = (list: TouchList): Touch | null => {
      if (activeId === null) return null;
      for (let i = 0; i < list.length; i++) {
        if (list[i].identifier === activeId) return list[i];
      }
      return null;
    };

    const onStart = (e: TouchEvent) => {
      if (activeId !== null) return; // already tracking a finger on the strip
      const t = e.changedTouches[0];
      if (!t) return;
      activeId = t.identifier;
      wasPlaying = store.getState().playing;
      pausedByDrag = false;
      seekToClientX(t.clientX);
    };

    const onMove = (e: TouchEvent) => {
      const t = findActive(e.touches);
      if (!t) return;
      e.preventDefault();
      // First actual movement: this is a scrub, not a tap — pause for it.
      if (!pausedByDrag && wasPlaying) {
        store.getState().togglePlay();
        pausedByDrag = true;
      }
      seekToClientX(t.clientX);
    };

    const onEnd = (e: TouchEvent) => {
      if (!findActive(e.changedTouches)) return; // not our finger
      if (pausedByDrag && wasPlaying) store.getState().togglePlay();
      activeId = null;
      pausedByDrag = false;
      wasPlaying = false;
    };

    canvas.addEventListener('touchstart', onStart, { passive: false });
    canvas.addEventListener('touchmove', onMove, { passive: false });
    canvas.addEventListener('touchend', onEnd);
    canvas.addEventListener('touchcancel', onEnd);
    return () => {
      canvas.removeEventListener('touchstart', onStart);
      canvas.removeEventListener('touchmove', onMove);
      canvas.removeEventListener('touchend', onEnd);
      canvas.removeEventListener('touchcancel', onEnd);
    };
  }, [store]);

  return <canvas ref={canvasRef} className="minimap" style={{ touchAction: 'none' }} />;
}
```

- [ ] **Step 2: Type-check**

Run: `npx tsc --noEmit`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add src/waveform/MiniMap.tsx
git commit -m "feat: minimap canvas (column cache, identifier-tracked scrub)"
```

---

## Task 4: Mount + styles + fix dpr rounding + docs

**Files:**
- Modify: `src/screens/Player.tsx`, `src/ui/styles.css`, `src/waveform/WaveformCanvas.tsx`, `CLAUDE.md`

**Interfaces:**
- Consumes: `<MiniMap />` (Task 3).
- Produces: minimap visible and interactive under the main waveform; docs match reality.

- [ ] **Step 1: Mount in `src/screens/Player.tsx`**

Add the import next to the `WaveformCanvas` one:

```tsx
import { MiniMap } from '../waveform/MiniMap';
```

and render it directly under the main waveform:

```tsx
      <WaveformCanvas />
      <MiniMap />
      <TempoControl />
```

- [ ] **Step 2: Add `.minimap` to `src/ui/styles.css`**

Insert right after the existing `.waveform` rule:

```css
.minimap { display: block; width: 100%; height: 48px; background: #0c0e12;
  border-top: 1px solid #23262f; }
```

- [ ] **Step 3: Fix the fractional-dpr reallocation in `src/waveform/WaveformCanvas.tsx`**

Pre-existing bug the minimap would otherwise double. Replace:

```ts
      if (canvas.width !== cssW * dpr || canvas.height !== cssH * dpr) {
        canvas.width = cssW * dpr;
        canvas.height = cssH * dpr;
      }
```

with:

```ts
      // canvas.width is an unsigned long: assigning a fractional cssW*dpr
      // (Android dpr 2.625/2.75) truncates, so the !== comparison stays true
      // forever and the backing store is reallocated every frame. Round first.
      const bw = Math.round(cssW * dpr);
      const bh = Math.round(cssH * dpr);
      if (canvas.width !== bw || canvas.height !== bh) {
        canvas.width = bw;
        canvas.height = bh;
      }
```

- [ ] **Step 4: Update `CLAUDE.md` — it is now actively wrong**

Three edits:

1. In "Out of scope for MVP", the paragraph claiming markers and the mini-map are
   not implemented and that `persistNow` always saves `markers: []` — replace it with:

```md
Marker UI and the mini-map are implemented (`src/ui/MarkersControl.tsx`,
`src/waveform/MiniMap.tsx`); `persistNow` saves the real `markers` array. A
Rubberband-based `AudioEngine` is a possible future engine swap; only
`SoundTouchEngine` exists today.
```

2. In "Commands", update the stale test count line to match the actual suite
   (run `npm test` and use the real numbers).

3. In the Architecture section's Waveform bullet, add:

```md
`MiniMap` is a second canvas (48px strip) showing the whole track — playhead,
loop, markers — with tap/drag scrub. It runs its own rAF loop but deliberately
does NOT call `store.tick()`: `WaveformCanvas` is the sole ticker (two tickers
would double-poll the engine clock).
```

- [ ] **Step 5: Type-check, full tests, build**

Run: `npx tsc --noEmit && npm test && npx vite build`
Expected: type-clean, all tests pass, build succeeds.

- [ ] **Step 6: Manual verify — REQUIRES touch**

`MiniMap` binds touch events only. A desktop mouse fires nothing, so use a real
phone (`npm run dev -- --host`) or DevTools device emulation with touch
simulation ON — otherwise this step chases a non-bug.

Expected:
- 48px strip under the big waveform shows the WHOLE track's waveform.
- Red playhead advances left→right across the strip during playback.
- A-B loop shaded; markers are amber ticks drawn over the playhead.
- Tap jumps there (single seek, no audible double restart); drag scrubs; playback
  resumes on release if it was playing.
- Put a second finger on the big waveform mid-drag, lift both — the strip must
  still respond afterwards (this is the dead-lock the review caught).

- [ ] **Step 7: Commit**

```bash
git add src/screens/Player.tsx src/ui/styles.css src/waveform/WaveformCanvas.tsx CLAUDE.md
git commit -m "feat: mount minimap; fix fractional-dpr canvas realloc; refresh CLAUDE.md"
```

---

## Self-Review

**Spec coverage (approved design):**
- Полный обзор дорожки (волна) → Task 1 `downsamplePeaks` + Task 3 column cache. ✓
- Playhead всей дорожки → Task 2 `overviewTimeToX` + Task 3. ✓
- A-B луп на стрипе → Task 3. ✓
- Маркеры на стрипе → Task 3 (рисуются последними). ✓
- Тап + драг = seek → Task 3 (identifier-tracked, pause deferred to first move). ✓
- Размещение под основной волной → Task 4. ✓
- Перф (не обходить full-track пики каждый кадр) → Task 3 column cache + dirty-check. ✓

**Plan-review findings, all folded in:** gesture dead-lock (identifier tracking),
vacuous downsample tests (exact values + non-divisible span), offscreen canvas
over-engineering (array cache instead), dpr cache-key staleness (moot), stale
CLAUDE.md (Task 4 Step 4), fractional-dpr realloc (Task 4 Step 3), idle rAF
(dirty check), touch-only manual verify (Task 4 Step 6), draw order (markers
last), double source restart on a bare tap (deferred pause).

**Placeholder scan:** none — every code step is complete.

**Type consistency:** `downsamplePeaks(peaks: Float32Array, columns: number): Float32Array`
identical in Tasks 1 and 3. `overviewTimeToX(t, duration, width)` /
`overviewXToTime(x, duration, width)` — same argument order in Tasks 2 and 3.
Store fields consumed by MiniMap (`peaks`, `duration`, `position`, `loopStart`,
`loopEnd`, `markers`, `playing`, `seek`, `togglePlay`) all exist in `PlayerState`
after the markers feature (`fcc9878`). `Marker` imported from `../types` for the
dirty-check type.

**Commits note:** per session rule the actual `git commit` steps run only after the user approves — batch the diffs and confirm.
