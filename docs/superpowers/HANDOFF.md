# Handoff — Разбор (Lena Audio Player)

**Обновлено:** 2026-07-15
**Статус:** Прод (`main`, `e9b2820`) — плеер переработан, **живёт и установлено на iPhone**, device-тест пройден. Редизайн библиотеки (13 задач, план `docs/superpowers/plans/2026-07-14-library-redesign.md`) **реализован и закрыт на ветке `feat/library-redesign`, гейты зелёные, но ветка ещё НЕ смержена в `main` и НЕ задеплоена** — device-тест по ней не проводился ни разу.
**Свежее:** библиотека переписана — `LibraryHeader` + `ThemeToggle`, карточки `TrackCard` (превью-волна `TrackWave`, время возобновления, бейджи не-дефолтных настроек), плавающая `ImportButton`-FAB, `TrackSheet` вместо нативного `confirm()`. Подробности реализации и что от неё осталось непроверенным — см. «Библиотека: что осталось проверить на устройстве» ниже. `HANDOFF-library-redesign.md` — это handoff-документ, с которым эта работа начиналась (контекст задачи, открытые вопросы брейншторма); он не обновлялся по ходу реализации, актуальный статус — здесь.

## 🚀 Прод

**<https://8ui.github.io/lena-audio-player/>** — GitHub Pages, деплой автоматом из Actions на пуш в `main`.
Репозиторий публичный (секретов нет — проверено сканом истории перед публикацией).

## Что это

Мобильная PWA для пианиста: снимать произведения на слух. Замедление/ускорение **без изменения тона**, транспонирование, A-B луп, крупный движущийся waveform, мини-карта, маркеры-закладки, офлайн-библиотека. Только телефон, portrait. Аудио **не покидает устройство** — всё локально в IndexedDB, бэкенда нет.

## Состояние

- `main` = `origin/main` на `e9b2820` (редизайн плеера), всё запушено, задеплоено, Actions зелёный.
- `feat/library-redesign` (эта ветка) — 13 задач редизайна библиотеки реализованы поверх `main`, не смержена. На HEAD этой ветки: **186 тестов** (27 файлов) · `tsc --noEmit` чист · `vite build` зелёный · бандл-проверки (`manifest.webmanifest` scope, `soundtouch-processor` в `sw.js`, `data-theme` в `index.html`) прошли.
- Все задачи в `docs/tasks/` закрыты (`rtp list` пуст).
- **Не проверено на устройстве** после редизайна плеера: читается ли янтарная заливка A-B лупа поверх янтарной волны (осознанный риск; лечится одной константой — `loopFill` в `theme.ts` + `--loop-fill` в `styles.css`, обе сразу, иначе падает тест парности).
- **Не проверено на устройстве вообще** после редизайна библиотеки — см. отдельный раздел ниже.

## Что сделано за последнюю сессию

| Коммит | Что |
|---|---|
| `9975c3e` | **M1:** декод-ошибки. Битый/неподдерживаемый файл падал молча — теперь `try/catch` + красный баннер. |
| `fcc9878` | **Маркеры.** Пур-логика `waveform/markers.ts`; add/remove/prev/next + persist; тики на волне. |
| `8055003` | **Мини-карта.** 48px стрип: вся дорожка, playhead, луп, маркеры. Кеш колонок вместо обхода десятков тысяч бакетов на кадр. Попутно фикс дробного dpr (реаллокация canvas 60/с на Android). |
| `0d862dd` | **`crypto.randomUUID` не существует вне secure context** — падал импорт И маркеры на телефоне по HTTP. Свой `uuid()` на `getRandomValues`. |
| `4028aad` | allowedHosts для cloudflared-туннеля (device-тест по доверенному HTTPS). |
| `a65fb53` | **iOS не давал выбрать mp3** — wildcard `accept="audio/*"` не резолвится в UTI. Явные расширения. |
| `fc04728` | **Мини-карта: относительный скраб** (тап больше не перебрасывает курсор) + убит iOS long-press callout. Жесты вынесены в **чистый редьюсер**. |
| `174afad`, `66d51a0` | **PWA + деплой:** настоящие иконки (генератор без зависимостей), iOS-мета, safe-area, subpath `/lena-audio-player/`, GitHub Pages через Actions. |

## Инварианты — НЕ СЛОМАТЬ

Каждый из них когда-то был реальным багом.

1. **`WORKLET_URL` (`SoundTouchEngine.ts`) — молчаливый убийца.** Под подпутём неверный путь → `SoundTouchNode.register` реджектится → **звука нет вообще, при идеально отрисованном UI**. Ни `tsc` (конфиг вне гейта), ни тесты (**vitest принудительно ставит `base: '/'`**) этого не видят. Защита: `vite.config` падает на кривом `base`, CI грепает боевой бандл. Не ослаблять.
2. **`WaveformCanvas` — единственный владелец `store.tick()`.** MiniMap читает `position`, свой тик не зовёт (два тикера дважды опрашивали бы часы движка).
3. **Оба canvas читают стор императивно** (`store.getState()` в rAF), НЕ через React-селектор: `position` меняется 60/с.
4. **Жесты — только `e.targetTouches`**, никогда `e.touches` (тот считает пальцы по всему экрану → палец на мини-карте ложно включал pinch-зум на волне). Оба canvas слушают `touchcancel`.
5. **`source.onended` — guard по идентичности ноды**, не boolean-флагом (`stop()` шлёт `ended` асинхронно).
6. **Темп = `playbackRate` на source И на node**, оба сразу. Позиция считается формулой, не репортится из worklet.
7. **zustand v5:** мульти-полевой селектор → `useShallow`.
8. **Логику — ВОН из canvas.** Жесты мини-карты давали HIGH-баг в каждой версии, пока жили внутри компонента. Теперь чистый редьюсер + тонкий адаптер. Со следующим жестом — так же.

## Осталось (по приоритету)

### 0. Смержить и задеплоить редизайн библиотеки, потом пройти device-тест
`feat/library-redesign` реализован (все 13 задач, гейты зелёные — см. «Состояние» выше), но живёт только на ветке. Следующий шаг — не код, а: смержить в `main`, задеплоить, и пройти список из «Библиотека: что осталось проверить на устройстве» ниже. До этого момента считать редизайн библиотеки непроверенным, несмотря на зелёные тесты.

### 1. Рассинхрон звука и волны — известное ограничение, не случайность
SoundTouch буферизует внутри (FIFO), а наша позиция — чистая формула, описывающая **вход** в worklet, а не **выход в динамик**. Итог: **playhead опережает звук**. Чинится вычитанием калиброванной задержки: `ctx.baseLatency + ctx.outputLatency` (запрашиваются) + внутренний буфер SoundTouch (**надо мерить**). Не угадывается — нужна задача с замерами на устройстве.

### 2. Перезагрузка при возврате на iOS
iOS выгружает фоновую вкладку и выполняет её заново. **Запретить нельзя** — решение ОС; мы жирная мишень, потому что держим декодированный `AudioBuffer` целого трека (5 мин стерео 44.1кГц ≈ **~100 МБ**).
Лечение симптома (в нашей власти): сохранять `currentTrackId` и восстанавливать на `init()` — сейчас после перезагрузки выкидывает в библиотеку. Тогда возврат станет почти незаметным: трек сам откроется на той же позиции.
Полное лечение (не держать весь трек в памяти) — переделка движка, отдельная большая тема.

### 3. Робастность из спеки (раздел «Обработка ошибок»), не закрыто
- Переполнение квоты IndexedDB → catch + «удали старые треки» (`db.ts` без catch).
- Feature-detect AudioWorklet на старом браузере → сообщение (сейчас белый экран).
- Долгий `computePeaks` на больших файлах → чанки + прогресс (сейчас синхронный, морозит UI).
- iOS suspended `AudioContext` → `ctx.resume()` есть, UX «нажми play» нет.

### 4. Rubberband — ЖДЁТ решения
Вторая реализация `AudioEngine` за тем же интерфейсом. Лечит «качество растяжки SoundTouch недостаточно». **Брать только если device-тест подтвердит, что артефакты слышны** — тяжёлый WASM + риск по батарее ради неподтверждённой проблемы это плохая ставка.

## Библиотека: что осталось проверить на устройстве

Редизайн (`feat/library-redesign`) прошёл все автогейты (тесты, `tsc`, `build`,
бандл-проверки), но **ни разу не открывался на реальном телефоне**. Зелёные
тесты здесь структурно не могут доказывать следующее (то же правило, что и
для плеера — см. CLAUDE.md):

- **Читаемость превью-волны (`TrackWave`) с 50–70 см.** Карточка рисует SVG
  из 48 столбиков (`BARS` в `TrackWave.tsx`) — не мерено на реальном экране
  под нужным углом обзора, только в браузере на столе.
- **Реально ли бэкдроп шита глотает тап по FAB.** `libraryLayers.test.ts`
  пришпиливает только z-index в CSS (`.sheet-backdrop` = 40 выше
  `.library-header`/`.import-fab` = 30) — это тот же трюк, что
  `ControlTabs.test.tsx` для плеера. jsdom не умеет хит-тестить стековые
  контексты, так что тест **не доказывает**, что открытый `TrackSheet`
  реально перехватывает тап по FAB на живом тач-экране — только что CSS
  написан так, что должен.
- **Чёлка/home indicator.** Не проверено, что `.library-header` не уезжает
  под вырез экрана и `.import-fab` — под home indicator на реальном iPhone
  (safe-area-inset учтён в коде, но не подтверждён визуально на устройстве).
- **Переключатель темы и КАНВАСЫ плеера.** `ThemeToggle` живёт в
  `LibraryHeader`, то есть тема переключается ДО того, как открыт трек.
  Канвасы плеера (`WaveformCanvas`, `MiniMap`) читают палитру через
  `activePalette()` заново на каждый кадр — по коду это должно работать без
  перезагрузки, но не проверено на устройстве, что вернувшись из библиотеки
  в уже открытый трек (или зайдя в трек после переключения темы) плеер
  реально перекрашивается, а не показывает кэшированные цвета.
- **Импорт `.mp3` из пикера на реальном iPhone.** `AUDIO_ACCEPT`
  (`src/ui/importAccept.ts`) с явными расширениями пришпилен тестом
  (`Library.test.tsx` / `ImportButton.test.tsx`), но это тест логики, не
  теста iOS-пикера — тот баг (`accept="audio/*"` гасил mp3 в UTI-маппинге)
  уже один раз ловился только на устройстве, не в тестах.

## Открытый долг (чекбоксы в `docs/tasks/*.md`, читаются `/debts-report`)

- `openTrack` при `rec == null` молча возвращает без ошибки юзеру.
- `addMarker` не дедуплицирует — повторные тапы дают накладывающиеся маркеры.
- persist→restore маркеров без автотеста (jsdom без AudioContext).
- A-B луп: скраб за пределы `[A,B]` на отпускании защёлкивается обратно в регион.
- MiniMap: размонтаж посреди drag не резюмит воспроизведение (практически недостижимо).
- Нет maskable-иконки → Android залеттербоксит в белый круг.
- wakeLock: гонка async request vs sync release; нет re-acquire на `visibilitychange`.
- Луп при B<A: движок корректно неактивен, но UI рисует регион.
- `db.ts`: singleton `dbp` не сбрасывается при reject `openDB`; `deleteTrack` не атомарен.

## Ключевые уроки этой сессии

- **Device-тест ловит то, что не поймает ничто другое.** 4 бага (`crypto.randomUUID`, iOS-пикер, абсолютный скраб, long-press меню) не видны ни в одном тесте и ни в одном ревью кода — только на живом телефоне.
- **Plan-review окупается на жестах и деплое.** Поймал: дедлок стрипа, протухший `wasPlaying`, отсутствие slop-порога, ложный pinch между canvas'ами, неребейзящийся `href` иконки, UI под часами.
- **Зелёные тесты ≠ работает.** Целые классы отказов (звук, base, жесты, PWA) структурно вне их досягаемости — это записано в CLAUDE.md, не полагаться на CI слепо.

## Процесс

Каждая задача идёт через `run-task-pipeline`: brainstorm → триаж пресета → (plan → plan-review) → impl (TDD) → review (verify + code-review) → done. Task-файлы в `docs/tasks/`, планы в `docs/superpowers/plans/`, спека в `docs/superpowers/specs/`.

## Локально

```bash
npm ci
npm test              # 186/186 (на feat/library-redesign; 123/123 на main)
npx tsc --noEmit      # чисто
npm run build         # + прогони build-гейт из .github/workflows/deploy.yml
npm run icons         # перегенерить иконки
npm run dev           # http://localhost:5173/lena-audio-player/  (base!)
```

**Device-тест по доверенному HTTPS** (нужен для SW/PWA/wakeLock/`crypto` — всё это secure-context only):

```bash
npx vite preview --port 4173 &
cloudflared tunnel --url http://localhost:4173
# открыть <tunnel>/lena-audio-player/ на телефоне
```
